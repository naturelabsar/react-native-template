import { StyleSheet } from "react-native";

export const defaultStyles = StyleSheet.create({
  mainButtonContainer: {
    borderRadius: 8,
    borderWidth: 1,
    borderColor: 'white',
    backgroundColor: 'blue',
  }
})